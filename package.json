{
  "name": "melodino",
  "version": "1.2.0",
  "private": true,
  "description": "Piano learning PWA for kids 5-12",
  "scripts": {
    "test": "npx vitest run",
    "test:unit": "npx vitest run tests/unit/",
    "test:e2e": "npx playwright test",
    "test:e2e:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "vitest": "^3.1.0",
    "jsdom": "^29.1.1"
  }
}
